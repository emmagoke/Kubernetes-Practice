apiVersion: batch/v1
kind: Job
metadata:
  name: counter
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
      - name: counter
        image: busybox:1.36
        command: ["sh", "-c", "echo processing; sleep 2; echo done"]
